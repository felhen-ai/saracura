"""Checkout-only, single-use TypeSafe native comparison tooling.

Nothing in this package is imported by the Saracura runtime or wheel.  The
module is deliberately stdlib-only: an operator may inject a credential only
into the short-lived child process which performs a separately authorised run.
"""
