[extend]
useDefault = true

[[allowlists]]
description = "Public identifier of the deterministic byte fixture, never a credential"
targetRules = ["generic-api-key"]
regexTarget = "match"
regexes = ['''fixture-bytes-v1''']

[[allowlists]]
description = "Public SHA-256 digest of the self-authored MiniLM conformance vector"
targetRules = ["generic-api-key"]
regexTarget = "match"
regexes = ['''240d6a4c4bba8b4a69321cd32586369834d3abb5691dd273adb1cddc07256664''']
